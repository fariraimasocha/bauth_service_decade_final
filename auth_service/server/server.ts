import {
    query,
    update,
    Canister,
    text,
    Record,
    StableBTreeMap,
    Ok,
    None,
    Some,
    Err,
    Vec,
    Result,
    nat64,
    ic,
    Opt,
    Variant,
} from 'azle';

import { v4 as uuidv4 } from 'uuid';

export default Canister ({

    

})